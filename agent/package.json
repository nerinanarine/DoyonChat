{
  "name": "doyonchat-agent-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "DoyonChat agent gateway (P3-010 Phase 0 PoC): pi coding agent RPC client with LF-only JSONL parsing",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:check": "tsc --noEmit",
    "test": "jest",
    "start": "npm run build && node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.42",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}